import { Meta } from '@storybook/addon-docs/blocks'
import { gradient } from '../../src/tokens/'

<Meta title="Tokens/Gradients" />

# Gradients

This is the documentation section about Gradients

---

<div 
  style={{
    display: 'grid', 
    gap: '2rem', 
    marginTop: '1.5rem', 
    width: '450px'
  }}
>
  {Object.entries(gradient).map(([tokenName,tokenValue]) => (
    <div>
      <h2 
        style={{ 
          borderBottom: 'none' 
        }}
      >
        {tokenName}
      </h2>
      <div 
        style={{
          display: 'grid', 
          gap: '0.5rem' 
        }}
      >
        <div 
          style={{ 
            height: '70px', 
            borderRadius: '4px', 
            boxShadow: '3px 2px 12px rgba(0,0,0,0.15)', 
            background: `linear-gradient(to right, ${tokenValue})`
          }}
        />
        <span 
          style={{ 
            fontSize: '0.75rem', 
            fontFamily: 'monospace'
          }}
        >
          {tokenValue}
        </span>
        <span 
          style={{ 
            fontSize: '0.75rem', 
            fontFamily: 'monospace'
          }}
        >
          --gradient-{tokenName}
        </span>
      </div>
    </div>
  ))}
</div>